/**
 * Root shell component. This renders only a placeholder chat screen for T1
 * (scaffolding). The real single-chat-interface UI (chat, memory, research,
 * writing, settings panels) is built in later tasks (T9, T17, T30).
 */
export function App(): JSX.Element {
  return (
    <main>
      <h1>논문 작성 서포터</h1>
      <p>채팅 화면을 준비 중입니다.</p>
    </main>
  );
}
